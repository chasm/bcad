To reset your credentials for the Bridge City Automotive website, please use the following coded link:

<%= @url %>

If you received this message in error, do nothing. This coded link will expire at <%= @admin["expires_at"].strftime("%l:%M %p on %A, %e %B") %>.